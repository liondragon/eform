- 2026-05-15: In greenfield mode with no operators or historical records, do not flag lack of backward compatibility as a defect. Prefer deleting obsolete schema fields, error codes, query params, aliases, and tests instead of preserving compatibility shims.
- 2026-05-29: In repeated simplify loops, re-audit wrappers added by the previous cleanup itself. One-consumer pass-through helpers and pre-checks around a locked canonical owner should be inlined or deleted before final verification.
- 2026-05-29: When planning refactors, identify repeated facts before repeated syntax. Move shared facts to the existing owner, migrate all consumers, delete superseded local paths, and add targeted negative seam guards; avoid new abstractions unless they have a natural owner and reduce ownership seams.
- 2026-06-15: For repeated address-findings passes, centralize the whole decision tuple at the owner, not just the newest duplicated field. If a caller needs `count`, `threshold`, flags, and delivery metadata, derive them together once and pass a bounded context downstream; keep downstream validation only as a public-boundary guard.
- 2026-06-15: Do not add a parallel carrier for facts already present in the canonical payload. If `security.soft_reasons` is the durable submission fact, normalize it at the submission boundary for submission decisions and let email re-normalize that same payload as its outbound header-safety boundary, instead of introducing separate mail context state.
- 2026-06-22: In active-carrier repos, avoid checkbox-only plan churn as verification. If code, tests, and narrow carriers already prove the work, leave transient plan ledgers unchanged unless the user explicitly asks to maintain them.
- 2026-06-22: For manual spam-feedback workflows, suspect-only capture misses zero-signal spam that got through. If the operator goal is to label delivered spam, plan opt-in all-delivered capture with bounded retention and privacy gates rather than partial capture that cannot support the stated review action.
- 2026-06-22: For review workflows, do not split pages by storage source when the operator job is the same. Prefer one review surface with source/provenance tabs or filters while keeping storage owners separate behind it.
- 2026-07-20: Audit retry safety across every single-use boundary in the pipeline, not only local ledger and file commits. When an external verifier supports idempotent retries, bind its idempotency key to the existing stable submission identity and test the outbound request shape.
- 2026-07-25: For visual alignment bugs, do not close on CSS box geometry alone. Reproduce the rendered route, capture browser screenshots in affected engines, inspect the pixels/glyph bounds when the complaint is optical, and ensure any optical correction does not move stable text between states.
- 2026-07-25: For visitor-facing recovery UX, do not expose backend reconciliation states or epistemic uncertainty as controls/copy. First decide the safe next user action (fix, retry with the primary action, wait, reload/contact), then keep status checks and recovery internals silent unless the user truly must choose them.
- 2026-07-20: A bounded GC limit is insufficient if discovery restarts at the same prefix. Persist a small owner-controlled cursor across applying runs, keep dry-runs non-mutating, and separate any full reconciliation scan into an explicit repair command.
- 2026-07-20: A strict persisted-state reader must validate relationships as well as field types: path identity, unique and disjoint logical IDs, policy bounds, accounting totals, and lifecycle timestamp ordering. Exercise those invariants through public owner methods so semantically corrupt state fails closed before mutation or serving.
- 2026-07-20: A full accounting repair must take a stable writer-aware snapshot. Under the established global-to-aggregate lock order, distinguish unresolved reservations from already-committed files and count each logical item exactly once; serialize zero-delta moves that could otherwise make a full-tree scan miss or duplicate bytes.
- 2026-07-20: When a protocol owner covers shared names, include response properties as well as request headers, parameters, hidden fields, and DOM attributes. Keep storage DTOs domain-shaped, map the public DTO at the endpoint, and emit the same response-name map to browser consumers.
- 2026-07-20: Model capacity ownership as an exclusive state machine across reservations, active items, and pending tombstones. Every delete and GC transition must replace one contribution with the next, remove matching reservations in the same locked update, and make the file-delete/capacity-write/manifest-marker crash windows explicitly retryable.
- 2026-07-20: When bounded history is required to make a later cleanup safe, reserve that history capacity when accepting the resource rather than discovering exhaustion during deletion. Collect manifest-less state only when its exact partial layout and maximum possible lifecycle age prove that no valid participant can still resume it.
- 2026-07-20: Never persist one half of a credential pair across a lifecycle boundary. If a browser-restored token can identify durable state but its matching secret is intentionally document-scoped, disable token reuse for that workflow and remint before the next state-creation attempt.
- 2026-07-20: A read-only consumed-token check is not a mutation guard. When resource creation and one-time finalization race, serialize the final unused check and creation through the same ledger lock that owns reservation; keep presentation-only fetch failures out of durable item state.
- 2026-07-20: Treat credential-backed browser restoration as an explicit blocking lifecycle phase. Retain credentials and offer retry for ambiguous transport or malformed-response failures, apply only a fully validated snapshot, and reserve terminal teardown for definitive server state or expiry. Enforce cheap upload-error and actual-size bounds before hashing even on idempotent retry paths.
- 2026-07-20: Capacity reconciliation must preserve identity as well as totals. If reservation-owned bytes have materialized without their manifest transition, retain that attributed reservation until retry or aggregate GC removes the files; and keep derivative attempts off disk until they fit the reserved committed-size bound.
- 2026-07-20: A destructive managed-state purge must take the global lock and pre-acquire every participant lock before deleting anything. On contention, fail closed and preserve both data and the global lock inode so active writers cannot split synchronization state.
- 2026-07-20: Recovery lookup must derive a direct, stable destination from the submission and input ordinals; never recover by scanning an age-growing history tree. If a pre-ledger claim must be reopened, perform that mutation under the ledger owner's unused-token lock so reservation cannot race the restoration.
- 2026-07-20: Cross-platform destructive cleanup must close aggregate file handles before deleting their roots. Make that safe by first establishing the durable purge barrier while all participant locks and the global lock are held, then retain the global lock through deletion.
- 2026-07-21: When the same helper call appears in read and mutation paths, qualify manual patches with the owning method context and immediately inventory every call site. Broad textual replacement can silently invert lifecycle semantics even when syntax and focused happy-path tests pass.
- 2026-07-21: When consolidating encoded media fixtures, validate the decoded byte length, MIME, and dimensions before replacing consumers. A single missing base64 quartet can preserve the magic bytes while corrupting the image structure and obscuring the source of downstream policy failures.
- 2026-07-21: When an uncommitted working file disappears during a concurrent audit, search Git's unreachable blobs and agent snapshots before restoring the index version. A worktree restore can discard unstaged user edits even when the staged baseline is preserved; recover the exact blob first, then reapply only verified local patches.
- 2026-07-21: Put mandatory human review before production mutation in migration plans, not in the cleanup task afterward. Inventory CMS-held activation state such as page shortcodes during preflight so a green source/template migration cannot be mistaken for a live route activation.
- 2026-07-22: When physical transfer runs outside aggregate locks, a logical tombstone alone cannot prevent a delayed writer from recreating released bytes. Serialize write and absence under one stable object lock, retain the deletion fence through normal aggregate GC because a previously authorized writer may not have reached that lock yet, and permit fenced-root removal only behind the exclusive lifecycle purge barrier. Prove the wait and the post-GC case rather than only a sequential late-write test.
- 2026-07-22: Free-space reservations must durably include future transient allocations across every concurrent pre-authorized transfer, not merely add the current request's scratch bytes to one observation. For bounded binary parsers, cap attacker-declared table entries and cumulative child records before iteration; a box-count limit alone does not bound work inside one box.
- 2026-07-22: Retry success paths must perform the same durable capacity validation or repair as the primary transition. Every positive-byte reservation must also retain the physical object locator needed for deletion recovery and reconciliation, so materialized bytes are attributed exactly once instead of becoming untracked or double-counted.
- 2026-07-22: A read-only absence scan cannot justify releasing capacity while an authorized writer may still start. Reconciliation must serialize absence under the stable object lock and leave the durable deletion fence in place. Binary-container acceptance must also bind metadata to a supported item definition and a bounded, nonempty payload extent rather than trusting dimensions alone.
- 2026-07-22: A genuine single-item HEIF fixture does not prove ordinary phone compatibility. Tiled HEIC commonly uses an `idat`-backed `grid` primary plus `dimg` references to media-backed HEVC tiles; prove that bounded dependency structure with a representative fixture before advertising HEIC generally.
- 2026-07-22: After reconciliation releases an absent intent reservation, later deletion must treat that intent as already uncharged. A synthetic delete reservation is valid only for a committed artifact whose bytes remain in the total; recreating one for a reconciled intent can subtract unrelated committed usage.
- An expired durable upload intent cannot merely return a conflict: because intents are intentionally absent from restored browser state and finalization requires zero intents, exact reauthorization must drive the expired ID through the existing tombstone/object-fence/capacity-release protocol. Keep that ID terminal so delayed completion cannot resurrect it; retry uses a fresh logical ID.
- A bounded HEIF primary-item parser is not automatically a still-image validator: explicitly reject sequence brands and top-level track/fragment containers, because otherwise valid primary metadata can hide an animated or multi-frame artifact beside ignored track data.
- Container structure and media extents do not prove a HEIF item is directly usable: parse `item_protection_index` for every supported `infe` version and reject nonzero values before committing the artifact.
- Validate decoded provider/adapter fact types before calling strict string primitives such as `hash_equals()`; schema key presence alone does not prevent malformed arrays or objects from turning a fail-closed boundary into an uncaught PHP `TypeError`.
- When enforcing a media-brand family rule, enumerate the complete standardized family rather than only the common pair; HEVC still-image sequence rejection includes `hevc`, `hevx`, `hevm`, and `hevs`.
- Before adding a new retry/idempotency mechanism to an implementation plan, check existing ADRs and runtime owners for already-settled idempotency boundaries. Prefer explicit ambiguous-response copy plus existing guards over reopening removed persistence machinery unless the new store has a named owner, schema, fingerprint contract, and production evidence.
- When planning browser recovery after ambiguous network failures, separate facts the browser actually receives from facts only the server owns. Do not say JavaScript can restore controls based on ledger, email, or retry state unless a received response or authenticated status endpoint exposes that exact fact; otherwise define a deterministic fallback table.
## 2026-07-22 — Persist display-oriented image facts and surface rejected-artifact cleanup failures

- JPEG dimensions from `getimagesize()` are coded dimensions; authoritative facts used by browser layout must apply bounded EXIF orientation parsing before persistence.
- When validation rejects an artifact after durable authorization/materialization, the cleanup result is load-bearing. Returning only the validation error can hide a retained intent or reservation and strand finalization.

## 2026-07-22 — Keep public upload limits and completion states server-authoritative

- When a server-owned artifact ceiling narrows a template limit, derive the effective tuple once and use it in the manifest policy, renderer attributes, and create/status responses. The browser should validate and apply that response before sending a body so displayed limits cannot drift from accepted limits.
- Browser transfer progress reaching 100% proves only that request bytes left the browser. Keep a distinct verifying state until the durable manifest commit response arrives, and restore unresolved durable intents as removable recovery cards rather than pretending they were committed or silently dropping them.
- When replacing a persisted media artifact model, migrate every lifecycle consumer in the same slice: signed review routes, download dispositions, GC byte labels, readiness diagnostics, uninstall roots, and runtime fixtures. Leaving any one derivative-shaped consumer behind keeps the retired model operationally authoritative even if the manifest is correct.
- For long canonical cryptographic fixtures, store compact payload digests and signatures, then reconstruct the envelope independently in each language. Copying full opaque tokens is fragile and can turn a transcription error into a misleading protocol failure.
- Deployment composition that selects materially different runtime paths belongs in explicit deployment constants and capability checks, not sanitized user configuration. Test immutable constant compositions in separate PHP processes, and make invalid explicit wiring fail closed rather than silently selecting a local fallback.
- A remote-storage health check must compare control-plane manifests with the capacity ledger, not reuse a local-file byte scan or S1 free-disk gate. Prove physical provider readiness separately through a signed non-customer data-plane operation, and keep lifecycle-management credentials in an operator-only verifier.
- A resumable remote uninstall needs the durable upload barrier before its drain clock becomes authoritative. Return immediately with a retry time, keep object identities in manifests rather than the drain record, and perform provider calls outside capacity and aggregate locks; only the existing purged barrier represents completion.
- Control-plane backup is incomplete evidence until restore revalidates the exact immutable provider version. Add a read-only exact-object operation, recover review prerequisites and conservative accounting from restored manifests, then prove ordinary manifest-driven deletion still releases the charge exactly once.
- When one signed capability selects equivalent local and remote representations, keep the action and recipe vocabulary in the shared protocol owner rather than either concrete provider. Provider code may own its physical recipe mechanics, but authorization, tests, and gallery routing should consume one protocol identity.
- A conditional object write alone does not make response-loss retries safe: a losing request can observe the winner's bytes before inspection completes. Publish a small exact-version validation fence only after bounded inspection, derive retries from that fence, and let only pre-fence creators perform request-local cleanup. Remote media acceptance must also run the same container invariants as local acceptance; a provider decoder proves decodability, not the product's still-image policy.
- Deployment composition owns only new aggregate creation. Every later authorization, transfer, completion, removal, review, repair, and cleanup operation must route from the aggregate's persisted immutable storage owner, so a rollout cannot strand retained batches. Independent GC families must continue under their own bounds when one provider-backed family fails, while the overall run remains observably unsuccessful.
- Durable operation fingerprints must identify stable resources, not rotating credentials; otherwise routine key rotation can strand a recoverable purge or migration. When a protocol spans PHP and Worker runtimes, generate and verify the Worker subset of fixed bounds from `src/Anchors.php`, and apply shared timing tolerances symmetrically at both verification boundaries.
- Remote capacity must retain the global object budget without inheriting local filesystem headroom checks; selecting zero transient bytes is not enough if the reservation owner still calls `disk_free_space()`. For cross-file release protocols, persist the idempotency phase before settling the ledger, make retry repair a retained reservation without repeating remote deletion, and teach aggregate expiry to distinguish already-released bytes so it cannot debit unrelated usage.
- A successful conditional marker write whose response is lost is still a durable publication. Before request-local cleanup deletes a newly created object, re-read and validate its exact-version marker; otherwise a concurrent retry can mint a valid receipt for bytes that cleanup removes moments later.
- Health and recovery must classify retained aggregates from each manifest's persisted immutable storage owner, never from the deployment composition used for new uploads. Mixed local/remote retention is a normal rollout state; prove both combined consistency and owner-specific loss detection.
- Fallback truth must not depend on stylesheet availability. Render failure-only status with the native `hidden` attribute and let the behavior owner reveal it after an actual load error; CSS may style that state but must not define whether it exists.
- Rate-limit defaults must be budgeted against the maximum supported request graph, not the old endpoint count. When one logical item intentionally consumes authorization plus completion attempts, preserve the documented per-request abuse gate and size the default for batch creation plus every advertised item phase.
- Reactivation invalidates an uninstall drain clock because new upload grants can be admitted after the barrier is removed. A later uninstall must establish a new barrier and full drain window even when a stale progress record remains; ambiguous browser deletion likewise requires both committed-item and pending-intent absence before removing the card locally.
- A provider class is not a complete persisted owner: retain the stable deployment/resource fingerprint with every remote aggregate and require it before upload, review, inspection, or deletion, or an endpoint rollover can turn “absent in the new bucket” into destructive false confirmation. Capacity reservations likewise need an explicit storage owner so remote byte claims stay in the global object budget without consuming local disk headroom.
- A terminal upload tombstone may legitimately own zero bytes and no capacity reservation. Recovery must require an accounting mutation only when positive bytes were charged; otherwise pre-authorization cancellation becomes a false corruption signal. Keep the first provider operation inside the same guarded failure boundary as the rest of a remote transaction, and give privacy-safe download names a MIME-derived extension so retained artifacts remain usable outside the browser.
- An object-store `put()` failure is ambiguous: the write may be durable even when its response is lost. Reopen and validate the exact immutable object within that request, but reserve request-local deletion for a definitively acknowledged creator. If validation-marker state cannot be read during error cleanup, preserve the object and defer to exact GC or the lifecycle backstop instead of turning uncertainty into deletion.
- Rollout readiness must derive required dependencies from both the new-write composition and every retained aggregate owner; a single deployment flag cannot prove old local multipart batches or old Worker/R2 batches remain operable. Replaceable caches also compete for real disk: serialize a bounded physical cache allocation with capacity admission and existing transient claims, then perform conversion outside the lock. When a terminal intent retires an upload ID, status-confirmed absence must give the retained browser `File` a fresh ID before offering Retry.
- Integer-second expiry checks are inclusive at their acceptance boundary. Destructive remote drain timestamps must advance to the first strictly safe second, and GC must retain tombstones at equality. When a retry arrives with its PHP multipart temporary file already materialized, subtract that request's matching transient claim from the free-space projection; also give authorization and completion fetches abort ownership so removed cards release queue slots immediately.
- Do not reuse operational clock skew for user-facing bearer access: review grants expire at their exact signed deadline. A resumable remote purge also needs a per-request work bound, not merely a persisted cursor; process one code-owned aggregate page, checkpoint family/cursor progress, and require another supported uninstall retry before continuing.
- Durable transient capacity and currently materialized temporary bytes are different facts. An exact reservation retry may subtract temporary bytes only when the current request explicitly proves that allocation already exists; authorization recovery must retain the full future-copy headroom. Terminal resumable-operation checkpoints are likewise absorbing states: later retries must return completion instead of restarting traversal after an unrelated cleanup failure.
- Replaceable cache deletion still needs a durable identity fence when the cache root has never existed; returning early on absence leaves first publication free to race authoritative-object deletion. Optional processor readiness must also include the resource gates required by its actual full-decode path, not only codec availability.
- A distributed aggregate must durably bind storage-owner identity before its manifest exists, and destructive cleanup must checkpoint idempotent capacity release before deleting the last authority record. Reconciliation may then fence and remove stale pre-manifest objects through that persisted owner, while GC and uninstall retries distinguish an already-released aggregate from an uncharged one without double-debiting unrelated bytes.
- A signed provider health response is insufficient when retained aggregates bind a different provider identity. Carry every persisted storage-owner fingerprint through accounting health and compare it with the configured origin/environment before a fresh endpoint can satisfy rollout readiness.
- Durable object validation needs its own bounded single-producer lease when an object may outlive a failed request. Publish immutable facts behind a version-bound marker, release the lease on every handled failure, reclaim abandoned leases only after the original work window, and let exact cleanup remove all auxiliary records.
- WordPress-side grant issuance throttling does not bound replay of an already-issued edge capability. After signature verification and before provider work, apply the required native limiter to the signed intent identity; fail closed when the binding is missing and include it in signed readiness.
- A deletion fence that closes a late-producer race also needs a bounded retirement owner. Preserve it through the admitted-request grace, then let normal GC remove the fence, lock, identity directory, and empty shard together; otherwise a correctness fix becomes an unbounded inode leak.
- A destructive owner-discovery scan must validate the complete persisted record before classifying it as local or remote. Reading only the discriminator can turn ordinary corruption into a fail-open purge decision and orphan externally stored bytes.
- A GC pass dedicated to one residue type must treat other well-formed live states as non-candidates. In particular, preview-fence reclamation should ignore unfenced cache and in-progress directories while continuing to fail closed on malformed directories that actually contain a deletion fence.
- Browser `File.type` is advisory and commonly empty for HEIC/HEIF. Derive the bounded declaration from the already-accepted extension, keep byte inspection authoritative, and normalize the multipart part MIME without decoding or copying the whole image in JavaScript.
- A remote uninstall drain bound is only sound if admitted upload streams have an enforced cancellation deadline, not merely a deadline check after object storage returns. Provider failures must also act as a run-level circuit breaker for later provider-backed cleanup calls.
- Cookie-free direct upload requires a distinct browser origin, not only `withCredentials = false`: same-origin XHR still carries ambient cookies. Reject same-origin Worker wiring in both the deployment owner and browser transport validator.
- Destructive remote cleanup must enumerate locator authority from both the manifest and capacity reservations. A reservation can be the only durable evidence after a pre-manifest crash; delete its deterministic object key and verify the reservation snapshot is unchanged before releasing capacity or deleting the aggregate.
- Origin identity must use browser-equivalent canonicalization. Normalize default ports before same-origin checks and persisted deployment fingerprints so `https://host` and `https://host:443` cannot split security behavior from storage ownership.
- A bounded server-side preview semaphore needs matching browser admission. Queue local preview requests at the configured conversion concurrency and retry transient saturation only a fixed number of times; otherwise a normal multi-image gallery turns temporary contention into permanent-looking failure. Native `hidden` state must also remain authoritative over component display rules.
- Presentation bounds must cover every preview producer and consumer. Do not reread full authoritative artifacts merely to decorate restored customer cards, and enforce the shared output-byte ceiling on provider transforms before caching or streaming even when the provider omits `Content-Length`.
- Readiness must exercise required operational dependencies, not merely detect their bindings. A signed Worker health result should make a health-scoped limiter call before declaring upload storage ready, so a rejected or failing limiter cannot pass deployment diagnostics while every real upload returns unavailable.
- User-facing metadata must come from committed authoritative facts, not advisory declarations or display-name suffixes. This includes both MIME and download extensions across local and remote delivery. Presentation failure announcements must likewise report the item's current lifecycle state rather than implying commit success early.
- An idempotency checkpoint must be validated from facts it still owns. After a remote aggregate release removes its reservations, retry code cannot reconstruct the original release amount from the post-release ledger; the durable batch-and-provider-bound receipt is the absorbing authority until aggregate deletion finishes.
- A new deployable trust boundary is incomplete until its deterministic offline suite runs in canonical CI. Genuine-provider assertions must also consume the provider-verified facts returned by the protocol, especially where accepted aliases can intentionally differ from detected authoritative MIME.
- An optional preview must remain optional all the way through presentation. A browser-side preview scheduler and retry counter duplicate server admission and contradict a no-queue product boundary; let native lazy image loading request the signed preview once, let the provider semaphore bound conversion, and reveal the authorized download fallback on failure.
- A versioned persisted-state schema is not complete merely because all required fields are validated. Reject unknown keys at the top level and inside every owned nested record so retired provider URLs, preview/processing state, or future parallel authority cannot silently survive the corruption boundary.
- Cross-runtime protocol bounds and browser upload declarations must be projected from their canonical PHP owners. Generate Worker constants from `Anchors` and emit extension-to-MIME declarations from `UploadPolicy`; duplicated literals quietly become competing contracts.
- Optional presentation providers should receive an action-scoped artifact handle from `UploadBatchStore`, not resolve managed object paths themselves. Keep manifest authorization, storage ownership, and physical member validation behind the aggregate facade, and route intentional external redirects through the WordPress runtime owner.
- A resource-owning function with many early returns should acquire handles into one visible state and release them in `finally`. Repeated branch-local lock and lease cleanup can be correct today while remaining the easiest place for the next failure branch to leak.
- A distributed lease cannot use read-then-delete for takeover or release: either gap can delete a successor or admit two owners. Use exact-etag conditional state transitions for active takeover, cleanup ownership, and released state so the provider chooses one winner atomically, and force both losing interleavings in tests.
- Keep accepted media policy singular inside each runtime, but do not auto-project provider capability from application acceptance. PHP may own product acceptance while the Worker owns the subset it has independently proved it can inspect and deliver; centralize each and require coordinated tests when either changes.
- Removing an automatic presentation retry queue must not make transient saturation terminal. Preserve the lean no-scheduler boundary while exposing an explicit user-driven retry that reloads the same signed preview URL and always keeps the authoritative download available.
- Changing a request graph requires validating retained operator limits, not only raising fresh-install defaults. Derive the minimum staged throttle budget from active field capacity and code-owned per-batch/per-item request counts; fail readiness with an actionable requirement instead of silently weakening an intentional security setting.
- Intermittent WordPress `rest_no_route` responses can mean the plugin aborted bootstrap for only that request, not that route registration or the web/PHP daemon cache is stale. Reproduce under concurrency and inspect request-time guards before changing service configuration; compatibility probes that mutate shared storage belong at activation/readiness boundaries and must use operation-unique paths.
- Every entry examined by a bounded cursor scan consumes budget, including well-formed non-candidates. Check the limit after classification rather than only after mutation, and retain the last examined cursor so a stable live prefix cannot starve eligible cleanup behind it.
- A manifest-less remote reservation can outlive its intent because a grant accepted at the last valid second may still own a bounded upload stream. Reconciliation must retain the locator through intent TTL plus upload duration and skew, and destructive owner discovery must inspect the capacity ledger as well as manifests before deciding no remote drain is needed.
- Environment-skipped browser tests still carry active assertions. When a restored presentation contract changes, update both self-contained coverage and configured live specs so enabling external credentials cannot resurrect an obsolete behavior expectation.
- Remote-owner discovery and remote purge must enumerate the same durable authorities. If capacity-only reservations can trigger an uninstall drain, the resumable purge needs a bounded final phase that deletes those exact locators before releasing their accounting or declaring completion.
- Shared browser runtimes need rendered fan-out and timer-transition tests, not only sequential single-card cases. A state flag is incomplete until every retained card rerenders its truthful controls, and a bulk action must enter work through the same single-writer boundary as individual actions. For optional previews, distinguish a one-shot admission queue from an automatic retry scheduler: assign one source at a time to match the nonblocking local conversion semaphore, but leave transient retry explicitly operator-driven.
- Operational acceptance prose is not an executable acceptance surface. Before claiming an operations phase is implemented, inventory each required provider, restore, lifecycle, and load command; distinguish deterministic control-plane drills from genuine-provider composition and leave the task open when the composed boundary has no runnable owner.
- Acceptance evidence must come from the owner of the asserted fact. Browser success can describe UX and request shape, but exact provider absence, released capacity, server decode work, and deployment identity require a privileged aggregate/provider observer. Exercise genuine provider failures through an explicitly gated disposable-deployment controller, not test switches in production runtime code.
- Request/retry evidence must begin at request admission, not at the response event. Otherwise aborted, DNS, connection, and response-loss attempts disappear from both the graph and retry count, making failure-heavy acceptance runs look artificially clean.
- A reusable integration core and a release-only fault matrix may share one executable, but their summaries and documented commands must stay distinct. Never let a zero-exit core run print language that implies an explicitly skipped provider-failure gate passed.
- Restore drills must bind evidence to every state named in the backup contract (open, finalized, delete-pending, capacity, and interrupted purge), and GC acceptance must reject nonzero per-aggregate errors even when the page-level result remains retryable.
- Provider-backed recovery evidence must materialize an object for every restored lifecycle state before backup and prove its later absence; deleting an invented or already-absent key verifies control flow, not recovery. Performance evidence likewise needs composition-aware companion-object counts, a bounded completion tail, and a closed deployed work graph rather than one ambiguous physical-item scalar.
- A dedicated release command must fail when its external inputs are absent even if the same spec is allowed to skip in the ordinary local suite. A deployment controller's `ready` claim is orientation, not proof: cross-runtime transitions need read-only probes from each configured owner plus assertions about the key that actually signs the response.
- Opaque evidence correlators must be fresh in a way the validator can prove. Syntax and same-workload echo checks do not reject a collector that reuses one token; retain a private run-local uniqueness set and never emit it.
- Paired-runtime evidence must bind the exact configured pair, not merely matching environment and key roles. Have one runtime emit a non-sensitive origin/environment fingerprint and require the other harness to derive the same value independently before accepting transition evidence.
- Register cleanup authority before a remote side effect that can commit without a usable response. Start with the supported wildcard locator, then narrow it to the verified exact version; otherwise response corruption can strand test artifacts outside the shutdown ledger.
- Exact evidence fields need names that identify the counted physical operation. An `R2 writes` count cannot silently mean only artifact-body writes when validation records and lease transitions also write R2 objects.
- Browser request accounting must classify artifact methods as well as routes. A cross-origin authorized upload necessarily generates CORS `OPTIONS` traffic on the same route; counting by route alone turns protocol setup into a successful artifact transfer.
- A fast disposable rotation matrix can prove transition mechanics but not a required overlap duration. Keep the duration in the code-owned constants registry and leave timestamped retention evidence explicitly open instead of implying consecutive controller phases exercised elapsed time.
- When removing presentation metadata from a UI payload, narrow the producing context, rendered tests, fixture markup, and operator-facing docs in the same pass. Otherwise stale labels or unused keys keep the retired presentation model alive as a test/documentation contract.
- When a host theme overrides plugin controls, make the narrow surface owner strong enough in the normal cascade and reuse one role-modified primitive across sibling controls. Repeated `!important` resets and pixel-exact tests only preserve the local conflict instead of establishing a reusable boundary.
- When component state needs screen-reader-only presentation, have the state owner toggle the shared accessibility utility and make component spacing conditional on that utility being absent. Copying the utility declarations creates a second owner and can still lose to later component rules in the cascade.
- When extending a shared configuration schema, reject a new key on consumers that do not implement it. A union-shaped allow-list can otherwise accept ignored configuration and create a misleading parallel contract.
- Correctness remediation must preserve the product value of the active public surface. Do not remove an intentional allowlisted projection, such as public non-private project summary fields, merely because a privacy or bounds finding exists; fix the owning validation/allowlist boundary instead.
- Hardening repair is still a mutation and must obey lifecycle barriers. When fixing protection files, split root/lock setup from content repair so runtime writers and purge leases do not rewrite private storage after a durable purge marker exists.
- Uninstall is an adapter, not a second policy owner. When cleanup must mirror a runtime sink's path and rotated-file rules, expose narrow owner methods from the sink and guard against uninstall-local path traversal or sibling matching.
- When a worktree already contains overlapping edits, inspect and patch one file at a time. A broad atomic patch built from stale context can fail wholesale and obscure which existing hunks are safe to preserve.
- A filesystem-permission contract must cover every path that creates, reads, probes, or repairs the directory, not only the primary writer. After changing modes, run the real health/GC paths and re-audit modes as the operator account; a generic diagnostic helper can silently restore an older permission invariant while still reporting PASS.
- Permission scope follows data classification, not a shared parent tree. Grant the trusted group access only to finalized review content; keep ordinary attachments, open artifacts, manifests, digests, locks, and fences owner-private, and test both the pre-review and authorized-review transitions.
- When doing mechanical test rewrites, inspect the resulting hunks before running broad suites. A broad replacement can leave syntactically incomplete arguments such as `limit =>` and waste verification cycles; patch one semantic pattern at a time or immediately run syntax over every touched file.
- Do not answer an unproven object-store cancellation edge with a mutable shadow lifecycle that still cannot make external I/O transactional. When the aggregate already reserves bytes, tombstones logical identity, delays remote cleanup across the grant/upload/skew drain, and retains the charge until confirmed absence, reuse that settlement owner. Explicitly distinguish application-managed capacity from exceptional provider residue when the API offers no settlement bound; do not claim a transactional physical ceiling. Add provider state only when it creates a new observable proof; asynchronous object validation normally needs the immutable object plus one immutable terminal result, with pending derived from result absence.
- Keep deployment compatibility out of persistent runtime protocol until simultaneous versions are a demonstrated requirement. A greenfield release can pause grants and drain incompatible validator or Queue changes operationally; bind policy inputs to the policy fingerprint, configure basic retry/DLQ alerting, and add migration identities or replay APIs only when they solve a live transition.
- For asynchronous handoffs, define admission, validation, commit, and cleanup cutoffs as ordered clock boundaries. A single vague expiry becomes contradictory once body transfer, durable publication, validation, and commit have different owners.
- When one signed capability supports both mutation and response-loss recovery, expiry alone is insufficient. Define time phases that monotonically narrow authority: create, recover by durable publication, recover from terminal facts, then close.
- When many independently signed item facts feed one atomic aggregate mutation, prefer one compact signed terminal snapshot with shared facts encoded once and revalidate it against current aggregate authority. Do not repeat per-item signatures or persist a second aggregate merely for transport.
- Do not introduce tenant, bucket-selection, sharding, or migration machinery into a single-deployment storage product. Pin one deployment-owned binding and use deterministic prefixes until an actual multi-tenant or migration requirement exists.
- When independent items become terminal at different times, a complete-set settlement recreates head-of-line blocking. Prefer serialized, idempotent subset envelopes: mutate each represented subset atomically, leave unrelated pending authority unchanged, and require exact all-matching replay for response-loss recovery.
- Browser upload progress and durable provider acceptance are separate milestones. Leave determinate progress immediately when the body is sent, release the transfer permit, and present the remaining provider acknowledgement and validation as Processing; otherwise correct asynchronous work still looks like a transfer stalled at 100%.
- Share upload plumbing at stable browser events, not by forcing storage providers into identical backend protocols. A common body-transfer/provider-request scheduler and UI reducer can serve synchronous local storage and asynchronous cloud storage while separate Anchor-owned in-flight bounds protect PHP and Worker resources.
- When a durable aggregate already owns exact intents and can call an external worker, prefer aggregate-owned pull reconciliation over making the browser relay signed outcomes. It removes browser authority and round trips while preserving localhost/private-origin operation and the existing outbound trust direction.
- A webhook or Queue callback is an acceleration path, not a complete recovery design, when the receiver can be unavailable longer than retries. If durable results still require pull reconciliation after an outage, ship the pull path first and add callbacks only for measured latency value.
- If scheduling authority is item-owned, one internal phase cannot span boundaries with different permit ownership. Model the minimum distinct phases needed to derive every permit count, then map multiple internal phases to one simpler visible state.
- When per-item authorization creates deadlines but transport batches terminal facts, keep deadlines per item and derive the compact envelope expiry as their minimum. The aggregate must still recheck every persisted item deadline after network I/O.
- Do not broaden exact deletion-authority assertions across authority families. Capacity-only Worker reservations can need `validation_until` for cleanup timing, while tombstone/finalized delete authority should keep its smaller exact-artifact shape.
- Before citing an existing performance contract as authority for a new gate, inspect its actual comparator and assertions. Reuse owned tolerances and caps, but state explicitly when the new design changes which scenarios are compared or when a recorded metric has no enforced limit.
- Never inspect raw WordPress or provider configuration files when a narrow command can answer the needed question. Config files may contain salts, credentials, or tokens; prefer sanitized owner commands and, if a direct read is unavoidable, filter to specific non-secret keys before displaying output.
- Place provider-specific admission barriers at the canonical mutation owner while holding that owner's lifecycle lease. A broad patch in shared authorization can accidentally affect unrelated transports or reference provider-only variables outside their valid scope.
- 2026-08-04: When a requested audit loop specifies partitioned/eluna leafs, partition before broad review. For capacity/accounting tests, treat “stale” failures as unresolved durability decisions until a counterexample review proves the new invariant; preserve global reconciliation proof unless a durable generation or touched-delta authority replaces it.
