# Owner Index

This is the canonical anti-drift rulebook for reusable owners, extension paths, forbidden local seams, and verification hooks.

Behavior authority lives in `docs/overview.md`, this owner map, `docs/Architecture_Router.md`, `docs/contracts/*`, affected owner docs/READMEs, code, and tests.

## Contract Carrier Completion Rule

For subsystem creation/deletion, shared runtime work, admin surface changes, config owner changes, persistence/storage-owner changes, public endpoint behavior, or cross-owner seams:

- update `docs/Architecture_Router.md` when the runtime center, subsystem boundary, dependency direction, or project doctrine changes
- update `docs/Owner_Index.md` when a reusable owner, extension path, forbidden local seam, or verification hook changes
- update `docs/overview.md` when operator-facing behavior changes
- update `docs/contracts/*` when stable public surfaces, template contracts, storage/runtime contracts, machine-readable outputs, or compatibility assumptions change
- update affected code-owner docs or READMEs when they exist

If one remains unchanged, the handoff must explicitly say why.

| Artifact / Feature | Canonical Owner | Allowed Extension Path | Forbidden Local Seams | Verification Hook |
|--------------------|-----------------|------------------------|-----------------------|-------------------|
| Real field types | `src/Validation/FieldTypeRegistry.php` | Add descriptor support through field-type classes, then expose through `FieldTypeRegistry::supported_types()` and handler registries. | Validator-local field type arrays; tests that treat `TemplateValidator` as the source of field-type truth. | `rg -n "FIELD_TYPES|supported_types|is_supported" src tests` |
| Form control, JS mint, and enhanced response protocol names | `src/FormProtocol.php` | Add shared PHP/JS names to `FormProtocol` and route PHP emitters/consumers or `forms.js` through its browser settings. | Local reserved-key maps in validators/controllers; hardcoded PHP↔JS mint or enhanced-response keys where both runtimes must agree. | `rg -n "reserved_keys\\(|RESERVED_KEYS|FormProtocol|eformsSettings\\.protocol|X-EForms-Response" src assets tests` |
| Enhanced final-submission response | `src/Submission/PublicRequestController.php` selects and shapes the public response; `src/FormProtocol.php` owns negotiated header and envelope names; `assets/forms.js` consumes it. `SubmitHandler` remains the submission pipeline, `Success` owns result locations, and `FormRenderer` owns fallback/error markup. | Extend structured `SubmitHandler` results, `Success` locations, `FormProtocol`, controller shaping, renderer metadata, and the existing browser runtime in that order. | A second submission endpoint/state machine; controller/browser validation or retry decisions; local wire-name maps; customer values, credentials, tokens, IDs, paths, signatures, or artifact facts in the JSON response; form/uploader replacement for an enhanced correctable or ambiguous response. | `php tests/integration/test_enhanced_submission.php && php tests/integration/test_form_protocol_contract.php && php tests/unit/test_protocol_seam_guards.php` |
| Public error-code messages | `src/ErrorMessages.php` | Add stable public error-copy mappings in `ErrorMessages`; keep `eforms_error_message()` as the public helper that delegates to it. | Duplicating public copy in renderers, submission handlers, result-page templates, or tests as a second runtime source. | `rg -n "ErrorMessages|eforms_error_message|EFORMS_ERR_EMAIL_SEND" src templates tests` |
| Structured errors and primary-code selection | `src/Errors.php` | Add structured error storage and deterministic primary-code selection in `Errors`; consumers may choose a fallback code. | Renderer- or submission-local traversal of `_global` and field error groups. | `rg -n "Errors::first_code|error_code_from_(result|errors)" src tests` |
| Validated template runtime context | `src/Rendering/TemplateContext.php` | Add runtime facts derived from an already validated template in `TemplateContext`; consumers use those facts directly. | Re-scanning validated field descriptors in renderers, endpoints, email, or submission orchestration to rediscover template-wide facts. | `rg -n "staged_field|TemplateContext::build|staged_fields\\(" src tests` |
| Row group pseudo-fields | `src/Validation/TemplateValidator.php` | Extend row-group validation/rendering as markup structure, not as a field descriptor. | Adding `row_group` to `FieldTypeRegistry`; descriptor handlers for row groups. | `rg -n "row_group|FieldTypeRegistry::supported_types" src tests` |
| Result-page query handling | `src/Submission/Success.php` | Extend `eforms_result`/`eforms_form` URL behavior in `Success`; consume result-page GETs through `PublicRequestController`. | Reintroducing form JSON page-template paths or arbitrary redirect destinations as live result-page routing. | `rg -n "eforms_result|Success::" src tests` |
| Result-page copy | `src/Submission/Success.php` | Add result-page title/message behavior through `result_pages.{success,email_failure}` in template JSON and consume it via `Success::get_result_title()` / `Success::get_result_message()`. | Reintroducing `success.mode`, `success.redirect_url`, external result destinations, or page-template-specific copy lookups. | `rg -n "success\\.mode|success\\.redirect_url|redirect_url|result_pages|Success::get_result" docs src templates tests` |
| Required WordPress runtime APIs | `src/WordPressRuntime.php` | Add fail-closed wrappers for WordPress APIs required by load-bearing runtime paths; tests may provide explicit stubs. | Raw PHP fallbacks for required WordPress APIs, especially redirect emission. | `rg -n "wp_safe_redirect|header\\( 'Location|WordPressRuntime" src tests` |
| POST size cap and request length | `src/Security/PostSize.php` | Add body-size cap calculation and request `Content-Length` extraction/comparison in `PostSize`; public, mint, upload, and submission adapters consume its result and keep endpoint-specific error/status shaping locally. | Endpoint-local `Content-Length` parsers or duplicated `length > cap` predicates in controllers/handlers. | `php tests/unit/test_post_size_cap_calc.php && php tests/integration/test_post_size_cap.php && php tests/integration/test_mint_endpoint_contract.php && php tests/unit/test_protocol_seam_guards.php` |
| Security-sensitive entropy | `src/Security/Entropy.php` | Route token, instance, request-id, and temp suffix entropy through `Entropy`; callers decide whether empty entropy is fatal or can fall back. | Per-owner CSPRNG wrappers or `mt_rand()` fallback paths. | `rg -n "random_bytes|openssl_random_pseudo_bytes|mt_rand|Entropy::" src tests` |
| Email meta production | `src/Email/Emailer.php` | Add produced email meta in `Emailer`; update template validation only for allowed `include_fields` references. | Treating email meta as generic form protocol state; duplicating produced meta maps in unrelated owners. | `rg -n "include_fields|submitted_at|submission_id|build_meta" src tests` |
| Operator review snapshot | `src/Submission/SubmissionReviewSnapshot.php` | Build and validate the managed-photo lead-review sidecar from `TemplateContext`, coerced values, submission ID, and submission timestamp; reuse descriptor-aware display formatting instead of rendered email output. | Raw POST or template rescans in review/admin paths; parsing email HTML/text; storing customer fields in `manifest.json`; treating the review sidecar as CRM delivery state or an all-submissions record. | `rg -n "SubmissionReviewSnapshot|Templates::render|default\\.html|default\\.txt|Emailer::send|review\\.json|manifest\\.json" src/Submission src/Uploads src/Admin templates tests` |
| Field render metadata | `src/Validation/FieldTypes/*` descriptors | Add render hints to field descriptors and let `FormRenderer` consume descriptor metadata. | Renderer-local field type buckets for behavior such as `enterkeyhint`. | `rg -n "enterkeyhint|last_textlike_index|descriptor_accepts_enterkeyhint" src tests` |
| Browser presentation assets and HTML attributes | `src/EformsAssets.php` owns plugin asset registration/enqueueing; `src/EformsMarkup.php` owns shared escaping and attribute serialization. | Add surface-specific bundles through `EformsAssets`; pass final control context into field renderers and serialize attributes through `EformsMarkup`. | Caller-local asset paths, versions, or duplicate enqueue logic; embedded page CSS/JS; duplicate attribute serializers; regex mutation of rendered controls. | `php tests/unit/test_eforms_assets.php && php tests/unit/test_eforms_markup.php && rg -n "inject_attributes|replace_name_attribute|attrs_to_string|<style>|<script>" src/Rendering src/Admin/SettingsAdmin.php` |
| Upload MIME and accept-token policy | `src/Uploads/UploadPolicy.php` | Extend accept tokens, MIME/extension mappings, and media bounds in `UploadPolicy`, then route validators/renderers/email policy through that owner. Keep box, association-entry, and cumulative-association bounds for decoder-free HEIC/HEIF container mechanics in the policy-private `HeifInspector`. | Local MIME maps or `application/octet-stream` fallbacks in `Validator`, renderers, or email code; direct `HeifInspector` calls outside `UploadPolicy`. | `rg -n "mime_allowed|TOKEN_MAP|HeifInspector|application/octet-stream" src tests` |
| Managed artifact object key | `src/Uploads/ManagedArtifactKey.php` and `worker/src/managed-artifact-key.js` | Derive the batch-scoped namespace, ordinal/intent filename, shard, and canonical extension from validated upload facts through these peers; keep the cross-language fixture and key tests synchronized. | Caller-local key concatenation or regexes; client filenames in object keys; submission-finalization moves/copies; MIME/extension aliases outside `UploadPolicy` and `worker/src/media-policy.js`. | `php tests/unit/test_managed_artifact_key.php && php tests/unit/test_worker_protocol.php && npm test --prefix worker` |
| Upload value shape | `src/Uploads/UploadValue.php` | Add pure shape, raw file-payload mapping, list, name, stored-path, and byte extraction helpers only; keep validation policy in `UploadPolicy`, storage in `UploadStore`/`UploadBatchStore`, and attachment policy in `Emailer`. | Local upload-item predicates or raw `$_FILES` shape walkers in normalizer, validator, store, email, or declined-review code; file I/O policy in `UploadValue`. | `rg -n "UploadValue::|is_upload_item|is_file_item|is_no_file|\\['name'\\].*\\['tmp_name'\\]" src tests` |
| Private upload root and lifecycle | `src/Uploads/PrivateDir.php` | Reuse private-root hardening, bounded symlink-safe directory pages, and the shared lifecycle lease across upload-family storage. Uninstall alone takes the exclusive nonblocking lease and establishes the durable purge barrier before deletion. | Caller-local private-root hardening or directory materialization; upload-owned token, ledger, throttle, synchronous, managed, or GC writes outside the lifecycle lease; caller-local purge-marker checks. | `rg -n "PrivateDir::|acquire_write_lease|acquire_purge_lease|mark_purged|managed-purged" src uninstall.php tests` |
| Submission-token use serialization | `src/Submission/Ledger.php` | Route ledger reservation and any mutation that must complete only while a token is unused through the stable ledger-root guard and ledger shard lock. | Read-then-mutate token checks in endpoints; ledger marker or lock writes outside `Ledger`; unlinking shard locks without the stable root guard; holding aggregate locks while reserving the ledger. | `rg -n "Ledger::reserve|Ledger::run_if_unused|delete_orphan_shard_lock|\\.used|ROOT_LOCK_FILENAME|SHARD_LOCK_FILENAME" src tests` |
| Managed upload aggregate, artifact, and capacity state | `src/Uploads/UploadBatchStore.php` | Route endpoints, submission finalization, review reads, operator deletion, GC, restore checks, and uninstall through the facade's intent/item/tombstone and purge operations. Persist each aggregate's immutable storage owner and exact Worker deployment fingerprint. Store-private `ManagedCapacityStore` owns capacity persistence, storage-owned exact artifact and transient-allocation reservation identity, and arithmetic; store-private `LocalArtifactStore` owns write-once local bytes and durable object-lock deletion fences. The Worker owns its write-once artifact plus exact-version validation fence, but no aggregate state. `UploadBatchStore` retains aggregate traversal and object-budget-before-aggregate lock ordering; physical transfer, inspection, preview production, and remote calls occur outside those locks, while the facade coordinates deletion, drain state, preview-cache fencing, and accounting. Normal aggregate GC preserves authoritative-artifact fences; `LocalPreviewProvider` removes preview-only fences after the shared safe-request grace. Whole-root removal is permitted only under the `PrivateDir` exclusive lifecycle lease and durable purge barrier. | Direct `manifest.json`, artifact, remote-purge, or managed-capacity writes in controllers, submission, email, templates, browser code, GC, or uninstall; calls to either private persistence collaborator outside `UploadBatchStore`; routing a retained Worker aggregate through a different deployment; charging Worker reservations against local free disk; bypassing an artifact, Worker validation, or preview-cache deletion fence; committing a first item without its exact intent reservation; dropping a pre-transfer local transient allocation claim; preview cache identity in aggregate state; batch indexes, reverse submission lookup, or per-caller state transitions. | `rg -n "UploadBatchStore|ManagedCapacityStore|LocalArtifactStore|LocalPreviewProvider|artifact_store_identity|remote-purge|manifest\\.json|managed-capacity|object_key|object_version" src templates uninstall.php tests` |
| Managed upload HTTP protocol | `src/Uploads/UploadBatchEndpoint.php` and `src/FormProtocol.php` | Add batch routes and route all shared header, hidden-field, parameter, transport, and response names through `FormProtocol`; let the endpoint select the deployment-bound transport, delegate state changes to `UploadBatchStore`, and delegate envelopes to `WorkerProtocol`. | Page-specific upload endpoints; batch credentials in URLs; endpoint-local protocol-name maps; browser-selected providers; manifest mutation in request handlers. | `rg -n "UploadBatchEndpoint|eforms_upload_batches|X-EForms-(Batch-Secret|Worker-Grant)|upload-batches" src assets tests` |
| Worker trust and ingress protocol | `src/Uploads/WorkerProtocol.php`, `worker/src/protocol.js`, and `worker/README.md` | Add direction-specific canonical envelopes through both protocol peers and shared fixtures; keep edge upload/CORS/storage/inspection behavior inside the Worker package and bind one composition through deployment constants. | JWT or signed JSON; `wp_salt()` reuse; browser-held signing keys; Config/admin/template provider switches; Worker aggregate mutation; provider URLs or grants persisted in manifests; local copies of envelope fields or signing rules. | `rg -n "WorkerProtocol|eforms-worker-(upload|review|health)|EFORMS_(UPLOAD_COMPOSITION|WORKER_)" src worker tests docs` |
| Worker outbound operations | `src/Uploads/WorkerClient.php` | Add bounded signed data-plane operations here; callers provide only exact manifest-owned identity and interpret safe result classes. Keep Cloudflare lifecycle-management credentials in the standalone operator verifier. | Controller-local HTTP/signing, WordPress cookies or auth headers sent to the Worker, provider management credentials in WordPress, provider URLs persisted in manifests, or remote calls while capacity/aggregate locks are held. | `rg -n "WorkerClient|wp_remote_post|EFORMS_CF_|remote_delete" src uninstall.php worker tests` |
| Managed review access and optional previews | `src/Uploads/ReviewController.php`, `src/Uploads/LocalPreviewProvider.php`, and `worker/src/index.js` | Own the versioned self-contained review-token codec and clean `/review/{token}` route, then validate each gallery/member bearer before reading finalized manifest `delete_after` authority. Keep gallery member URLs on WordPress without URL expiry; serve local downloads through the controller and mint a fresh exact-version Worker preview/download grant only when an authorized member URL is requested. Gate operator availability updates and whole-submission deletion through the opaque gallery URL, `manage_options`, a nonce, and `UploadBatchStore`. Route optional local conversion through the fixed-recipe provider and its per-object lock/global semaphore. | Review query aliases, token indexes or reverse lookups, public artifact/cache URLs, provider grants in manifests, email, or gallery HTML, preview work before bearer validation, URL-expiry as a second lifetime, direct artifact embedding, fallback from Worker preview to local processing, per-photo mutation routes, controller-local aggregate deletion, or preview state in submissions. | `rg -n "ReviewController|/review/|eforms_review|LocalPreviewProvider|/v1/review|review_grant|preview-cache|delete_after|delete_finalized_submission" src worker templates tests` |
| Config array-path reads | `src/Config.php` | Add strict shared lookup helpers such as `Config::value()`, `Config::bool()`, and `Config::has_path()`; keep coercion/clamp policy in existing consuming owners. | Local array-path traversal helpers; truthy parsing for boolean config reads. | `rg -n "Config::value|Config::bool|Config::has_path|foreach \\( \\$path|array_key_exists\\( \\$segment" src tests` |
| Spam content filter policy | `src/Spam/ContentFilter.php` | Add blocked-term parsing, normalization, duplicate detection, descriptor/type-driven scalar text selection, and matching here; submission, settings, diagnostics, logs, and email consume its result shape. | Adding content matches to `Security::soft_reasons`; parsing blocked terms in `SubmitHandler`, `SettingsFields`, `Emailer`, `Logging`, or diagnostics; regex UI for blocked terms. | `rg -n "content_filter|ContentFilter|blocked_terms|content_blocked_term" src tests docs` |
| Admin settings surface | `src/Admin/SettingsAdmin.php` | Add Settings -> eForms tabs, capability/nonce gates, admin notices, and save orchestration here; bootstrap may only register hooks. | Rendering settings in `DeclinedReviewAdmin`, `SubmitHandler`, `Config`, or bootstrap. | `rg -n "SettingsAdmin|add_options_page|eforms-settings|Settings -> eForms" src tests` |
| Admin settings option persistence | `src/Admin/AdminSettingsStore.php` | Read/write/delete the sparse `eforms_admin_config` WordPress option here, using `Config` validation before persistence; uninstall cleanup may call `AdminSettingsStore::delete_all()` but must not duplicate the option name. | Raw `get_option('eforms_admin_config')`, `update_option('eforms_admin_config')`, or `delete_option('eforms_admin_config')` calls in renderers, public runtime code, or uninstall cleanup. | `rg -n "eforms_admin_config|update_option|delete_option|get_option" src uninstall.php` |
| Admin config merge and provenance | `src/Config.php` | Add admin override validation, merge precedence, final snapshot, effective-config provenance, externally-controlled decisions, and secret masking here. | Source-label or precedence logic in admin render branches; parallel admin schemas outside `Config`. | `rg -n "source label|source_label|externally controlled|eforms_admin_config|validate_admin" src tests` |
| Admin settings field matrix and mapper | `src/Admin/SettingsFields.php` or equivalent admin settings-field owner | Keep labels, groups, control metadata, submitted-field sentinels, and the form-to-override mapper here; derive config path constraints from `Config`. | Per-group submit branches; duplicate enum/range/secret/nullability facts; local blank-secret or disabled-field preservation rules in `SettingsAdmin`. | `rg -n "settings field matrix|form-to-override|blank.*secret|secret.*blank|source label|source_label|externally controlled" src/Admin tests` |
| Spam smoke diagnostic engine | `src/Diagnostics/SpamSmokeDiagnostic.php` | Add spam smoke checks and result-shape changes here; CLI and admin may only adapt formatting, authorization, nonce, and exit behavior. | Duplicating smoke checks in `SpamSmokeCommand`, `SettingsAdmin`, tests, or scripts. | `rg -n "SpamSmokeDiagnostic|SpamSmokeCommand|Run Spam Smoke Test|spam-smoke" src tests` |
| Runtime health diagnostic engine | `src/Diagnostics/RuntimeHealthDiagnostic.php` | Add runtime health checks and result-shape changes here; CLI and admin may only adapt formatting, authorization, nonce, and exit behavior. | Duplicating storage, template, GC, CLI bootstrap, or config provenance checks in CLI/admin adapters. | `rg -n "RuntimeHealthDiagnostic|RuntimeHealthCommand|Run Runtime Health Check|wp eforms doctor" src tests` |
| Logging file-write mechanics | `src/Logging/FileSink.php` | Share locked append, chmod, size checks, JSONL line encoding, non-recursive file-family pruning/deletion mechanics, and dated JSONL family naming/matching/date extraction; callers supply prefix/extension, format, retention policy, and enablement. | Moving content schemas, retention policy, enablement, recursive multi-target GC traversal, or fail2ban `.1` sibling policy into the shared sink. | `rg -n "FileSink|append_with_rotation|next_rotated_path|FILE_PREFIX" src/Logging tests` |
| Declined-submission review storage | `src/DeclinedReviewLog.php` | Add declined-review capture, JSONL record shaping, bounded content retention, date-window reads, and detail lookup here; callers pass decision context only. | Writing submitted content through `Logging::event()`, DB-backed moderation state, or per-record mutation outside this owner. | `rg -n "DeclinedReviewLog::capture|declined_review|EFORMS_ERR_SPAM" src tests` |
| WordPress admin list pagination markup | `src/Admin/AdminListPagination.php` | Supply surface-owned item labels, page URLs, and only the controls supported by that surface; reuse the WordPress list-table classes without local CSS or JavaScript. | `paginate_links()` front-end markup, page-local pagination CSS, or invented admin navigation shells. | `rg -n "AdminListPagination|page-numbers|tablenav-pages|pagination-links" src/Admin tests` |
| Declined-review admin surface | `src/Admin/DeclinedReviewAdmin.php` | Add Tools-page filters, table/detail presentation, notices, and escaping here; bootstrap may only register hooks. | Admin rendering in `SubmitHandler`, `Logging`, or a custom frontend framework. | `rg -n "eforms-declined|DeclinedReviewAdmin|add_management_page" src tests` |
| Retained photo submissions admin surface | `src/Admin/SubmissionsAdmin.php` | Add the `manage_options` Tools page for currently retained managed-photo submissions; ask `UploadBatchStore` for traversal/storage facts and `SubmissionReviewSnapshot` for summary semantics. | Admin/template direct reads of `manifest.json`, `review.json`, or `submissions/`; all-submissions inbox, DB archive, CRM status, search/filter/export, or newest-first claims without an explicit storage index. | `rg -n "SubmissionsAdmin|eforms-submissions|manifest\\.json|review\\.json|submissions/" src/Admin templates assets tests` |
