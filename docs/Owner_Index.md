# Owner Index

Reusable owner map only. Behavior remains authoritative in `docs/Canonical_Spec.md`.

| Artifact / Feature | Canonical Owner | Allowed Extension Path | Forbidden Local Seams | Verification Hook |
|--------------------|-----------------|------------------------|-----------------------|-------------------|
| Real field types | `eforms/src/Validation/FieldTypeRegistry.php` | Add descriptor support through field-type classes, then expose through `FieldTypeRegistry::supported_types()` and handler registries. | Validator-local field type arrays; tests that treat `TemplateValidator` as the source of field-type truth. | `rg -n "FIELD_TYPES|supported_types|is_supported" eforms/src eforms/tests` |
| Form control and JS mint protocol names | `eforms/src/FormProtocol.php` | Add shared PHP/JS names to `FormProtocol` and route PHP emitters/consumers or `forms.js` through its browser settings. | Local reserved-key maps in validators/controllers; hardcoded PHP↔JS mint keys where both runtimes must agree. | `rg -n "reserved_keys\\(|RESERVED_KEYS|FormProtocol|eformsSettings\\.protocol" eforms/src eforms/assets eforms/tests` |
| Public error-code messages | `eforms/src/ErrorMessages.php` | Add stable public error-copy mappings in `ErrorMessages`; keep `eforms_error_message()` as the public helper that delegates to it. | Duplicating public copy in renderers, submission handlers, result-page templates, or tests as a second runtime source. | `rg -n "ErrorMessages|eforms_error_message|EFORMS_ERR_EMAIL_SEND" eforms/src eforms/templates eforms/tests` |
| Row group pseudo-fields | `eforms/src/Validation/TemplateValidator.php` | Extend row-group validation/rendering as markup structure, not as a field descriptor. | Adding `row_group` to `FieldTypeRegistry`; descriptor handlers for row groups. | `rg -n "row_group|FieldTypeRegistry::supported_types" eforms/src eforms/tests` |
| Result-page query handling | `eforms/src/Submission/Success.php` | Extend `eforms_result`/`eforms_form` URL behavior in `Success`; consume result-page GETs through `PublicRequestController`. | Reintroducing form JSON page-template paths or arbitrary redirect destinations as live result-page routing. | `rg -n "eforms_result|Success::" eforms/src eforms/tests` |
| Result-page copy | `eforms/src/Submission/Success.php` | Add result-page title/message behavior through `result_pages.{success,email_failure}` in template JSON and consume it via `Success::get_result_title()` / `Success::get_result_message()`. | Reintroducing `success.mode`, `success.redirect_url`, external result destinations, or page-template-specific copy lookups. | `rg -n "success\\.mode|success\\.redirect_url|redirect_url|result_pages|Success::get_result" docs eforms/src eforms/templates eforms/tests` |
| Required WordPress runtime APIs | `eforms/src/WordPressRuntime.php` | Add fail-closed wrappers for WordPress APIs required by load-bearing runtime paths; tests may provide explicit stubs. | Raw PHP fallbacks for required WordPress APIs, especially redirect emission. | `rg -n "wp_safe_redirect|header\\( 'Location|WordPressRuntime" eforms/src eforms/tests` |
| Security-sensitive entropy | `eforms/src/Security/Entropy.php` | Route token, instance, request-id, and temp suffix entropy through `Entropy`; callers decide whether empty entropy is fatal or can fall back. | Per-owner CSPRNG wrappers or `mt_rand()` fallback paths. | `rg -n "random_bytes|openssl_random_pseudo_bytes|mt_rand|Entropy::" eforms/src eforms/tests` |
| Email meta production | `eforms/src/Email/Emailer.php` | Add produced email meta in `Emailer`; update template validation only for allowed `include_fields` references. | Treating email meta as generic form protocol state; duplicating produced meta maps in unrelated owners. | `rg -n "include_fields|submitted_at|submission_id|build_meta" eforms/src eforms/tests` |
| Field render metadata | `eforms/src/Validation/FieldTypes/*` descriptors | Add render hints to field descriptors and let `FormRenderer` consume descriptor metadata. | Renderer-local field type buckets for behavior such as `enterkeyhint`. | `rg -n "enterkeyhint|last_textlike_index|descriptor_accepts_enterkeyhint" eforms/src eforms/tests` |
| Upload MIME and accept-token policy | `eforms/src/Uploads/UploadPolicy.php` | Extend accept tokens and MIME/extension mappings in `UploadPolicy`, then route validators/renderers/email policy through that owner. | Local MIME maps or `application/octet-stream` fallbacks in `Validator`, renderers, or email code. | `rg -n "mime_allowed|TOKEN_MAP|application/octet-stream" eforms/src eforms/tests` |
| Upload value shape | `eforms/src/Uploads/UploadValue.php` | Add pure shape, raw file-payload mapping, list, name, stored-path, and byte extraction helpers only; keep validation policy in `Validator`, storage in `UploadStore`, and attachment policy in `Emailer`. | Local upload-item predicates or raw `$_FILES` shape walkers in normalizer, validator, store, email, or declined-review code; file I/O policy in `UploadValue`. | `rg -n "UploadValue::|is_upload_item|is_file_item|is_no_file|\\['name'\\].*\\['tmp_name'\\]" eforms/src eforms/tests` |
| Config array-path reads | `eforms/src/Config.php` | Add strict shared lookup helpers such as `Config::value()`, `Config::bool()`, and `Config::has_path()`; keep coercion/clamp policy in existing consuming owners. | Local array-path traversal helpers; truthy parsing for boolean config reads. | `rg -n "Config::value|Config::bool|Config::has_path|foreach \\( \\$path|array_key_exists\\( \\$segment" eforms/src eforms/tests` |
| Admin settings surface | `eforms/src/Admin/SettingsAdmin.php` | Add Settings -> eForms tabs, capability/nonce gates, admin notices, and save orchestration here; bootstrap may only register hooks. | Rendering settings in `DeclinedReviewAdmin`, `SubmitHandler`, `Config`, or bootstrap. | `rg -n "SettingsAdmin|add_options_page|eforms-settings|Settings -> eForms" eforms/src eforms/tests` |
| Admin settings option persistence | `eforms/src/Admin/AdminSettingsStore.php` | Read/write/delete the sparse `eforms_admin_config` WordPress option here, using `Config` validation before persistence; uninstall cleanup may call `AdminSettingsStore::delete_all()` but must not duplicate the option name. | Raw `get_option('eforms_admin_config')`, `update_option('eforms_admin_config')`, or `delete_option('eforms_admin_config')` calls in renderers, public runtime code, or uninstall cleanup. | `rg -n "eforms_admin_config|update_option|delete_option|get_option" eforms/src eforms/uninstall.php` |
| Admin config merge and provenance | `eforms/src/Config.php` | Add admin override validation, merge precedence, final snapshot, effective-config provenance, externally-controlled decisions, and secret masking here. | Source-label or precedence logic in admin render branches; parallel admin schemas outside `Config`. | `rg -n "source label|source_label|externally controlled|eforms_admin_config|validate_admin" eforms/src eforms/tests` |
| Admin settings field matrix and mapper | `eforms/src/Admin/SettingsFields.php` or equivalent admin settings-field owner | Keep labels, groups, control metadata, submitted-field sentinels, and the form-to-override mapper here; derive config path constraints from `Config`. | Per-group submit branches; duplicate enum/range/secret/nullability facts; local blank-secret or disabled-field preservation rules in `SettingsAdmin`. | `rg -n "settings field matrix|form-to-override|blank.*secret|secret.*blank|source label|source_label|externally controlled" eforms/src/Admin eforms/tests` |
| Spam smoke diagnostic engine | `eforms/src/Diagnostics/SpamSmokeDiagnostic.php` | Add spam smoke checks and result-shape changes here; CLI and admin may only adapt formatting, authorization, nonce, and exit behavior. | Duplicating smoke checks in `SpamSmokeCommand`, `SettingsAdmin`, tests, or scripts. | `rg -n "SpamSmokeDiagnostic|SpamSmokeCommand|Run Spam Smoke Test|spam-smoke" eforms/src eforms/tests` |
| Runtime health diagnostic engine | `eforms/src/Diagnostics/RuntimeHealthDiagnostic.php` | Add runtime health checks and result-shape changes here; CLI and admin may only adapt formatting, authorization, nonce, and exit behavior. | Duplicating storage, template, GC, CLI bootstrap, or config provenance checks in CLI/admin adapters. | `rg -n "RuntimeHealthDiagnostic|RuntimeHealthCommand|Run Runtime Health Check|wp eforms doctor" eforms/src eforms/tests` |
| Logging file-write mechanics | `eforms/src/Logging/FileSink.php` | Share locked append, chmod, size checks, JSONL line encoding, generic pruning mechanics, and dated JSONL family naming/matching/date extraction; callers supply prefix/extension, format, retention policy, and enablement. | Moving content schemas, retention policy, enablement, or fail2ban `.1` sibling policy into the shared sink. | `rg -n "FileSink|append_with_rotation|next_rotated_path|FILE_PREFIX" eforms/src/Logging eforms/tests` |
| Declined-submission review storage | `eforms/src/DeclinedReviewLog.php` | Add declined-review capture, JSONL record shaping, bounded content retention, date-window reads, and detail lookup here; callers pass decision context only. | Writing submitted content through `Logging::event()`, DB-backed moderation state, or per-record mutation outside this owner. | `rg -n "DeclinedReviewLog::capture|declined_review|EFORMS_ERR_SPAM" eforms/src eforms/tests` |
| Declined-review admin surface | `eforms/src/Admin/DeclinedReviewAdmin.php` | Add Tools-page filters, table/detail presentation, notices, and escaping here; bootstrap may only register hooks. | Admin rendering in `SubmitHandler`, `Logging`, or a custom frontend framework. | `rg -n "eforms-declined|DeclinedReviewAdmin|add_management_page" eforms/src eforms/tests` |
