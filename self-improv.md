# Self-Improvement Log (Agent Process Learnings)

Meta-learnings about improving `AGENTS.md` and `agent_docs/*` (workflow, prompts, checklists).

Rules:
- Append-only: do not delete or rewrite prior entries.
- Logging here does not grant permission to edit `AGENTS.md` or `agent_docs/*` unless explicitly requested.

## Entries

- Date:
  - Context:
  - What went wrong / friction:
  - Improvement (actionable, specific):
  - Proposed doc target (`AGENTS.md` / `agent_docs/...`):
  - Follow-up (optional):

